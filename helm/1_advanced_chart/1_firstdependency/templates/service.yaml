apiVersion: v1
kind: Service
metadata:
  name: {{ include "1_firstdependency.fullname" . }}
  labels:
    {{- include "1_firstdependency.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "1_firstdependency.selectorLabels" . | nindent 4 }}
